{
  "name": "bulk-order-console",
  "private": true,
  "type": "module",
  "description": "Internal bulk corporate gift order console — Go-To Gifting LLC",
  "dependencies": {
    "@netlify/blobs": "^11.1.0"
  },
  "scripts": {
    "test": "node tools/stamp-assets.mjs && esbuild netlify/functions/*.mjs --bundle --platform=node --format=esm --target=node20 --outdir=tests/.build --log-level=warning && node tests/e2e.mjs && node tests/giftmatch.mjs",
    "stamp": "node tools/stamp-assets.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
